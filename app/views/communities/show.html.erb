<div class="level">
  <div class="level-left">
    <h1 class="title is-2"><%= @community.community %></h1>
  </div>
  <div class="level-right">
    <% if has_role?(:admin) %>
      <%= link_to 'Edit community', edit_community_path(@community), class:"button" %>
    <% end %>
  </div>
</div>
<hr />
<div class="columns">
  <%= render 'shared/discussions' %>
  <%= render 'discussions/sidebar' %>
</div>
